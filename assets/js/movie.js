export default class Movie {
  constructor(key) {
    this.key = key;
  }

  /**
   * get movies list
   */
  movies() {
    // ajax
  }

  /**
   * get details single movie
   * @params: id(Int)
   */
  find() {
    // ajax
  }

  /**
   * get discover list
   */
  discover() {
    // ajax
  }

  /**
   * get favorite movies
   * @params: arr(Array)
   */
  favorites(arr) {
    // get favorite movies
  }
}
